apiVersion: v2
name: k8s-demo-chart
description: A production-ready Helm chart for deploying the K8s-mini project consisting of frontend, backend, MongoDB database, persistent volumes, configmaps, secrets, and ingress resources.
type: application
version: 0.1.0
appVersion: "1.0.0"
