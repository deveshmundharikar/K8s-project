apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: myproject-ns
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
      nodePort: 30001
  type: NodePort
