name: Frontend CI

on:
  push:
    branches:
      - main
    paths:
      - 'demo-k8s-project/frontend/**'
      - '.github/workflows/frontend.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'demo-k8s-project/frontend/**'

jobs:
  frontend-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Generate Image Tag
        id: prep
        run: |
          # Create a clean, unique timestamp tag (e.g., 20260529-1544)
          TAG=$(date +'%Y%m%d-%H%M%S')
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          # Also export git commit SHA as a backup tag
          echo "sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Build and Push Frontend Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./demo-k8s-project/frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/frontend:${{ steps.prep.outputs.tag }}
            ${{ secrets.DOCKER_USERNAME }}/frontend:${{ steps.prep.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
